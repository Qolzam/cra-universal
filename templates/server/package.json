{
  "name": "create-react-app-server",
  "version": "1.0.0",
  "description": "Express server for Create React App",
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "start": "cross-env NODE_ENV=development webpack --watch",
    "start:prod": "cross-env NODE_ENV=production node build/bundle.js",
    "build": "cross-env NODE_ENV=production webpack -p --progress --profile --bail",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.4",
    "babel-preset-env": "^1.6.1",
    "cross-env": "^5.1.3",
    "nodemon-webpack-plugin": "0.1.6",
    "null-loader": "^0.1.1",
    "webpack": "^4.1.1",
    "webpack-cli": "^2.0.10",
    "webpack-node-externals": "^1.6.0"
  },
  "dependencies": {
    "@cra-express/core": "^2.1.0",
    "babel-plugin-dynamic-import-node": "^1.2.0",
    "dotenv": "^5.0.1"
  }
}
